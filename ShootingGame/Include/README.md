## 현재 진척도(어디까지 보았는가)
18화 플레이어 이동 및 몬스터

## 다음 영상
19화 오브젝트 리스트 설계 및 프로토타입 설계

## 예전 진척도
(2020 - 11 - 10) 
11화 프레임워크 프로젝트 설정 및 싱글톤

(2020 - 11 - 11) 
12화 Core Window 생성
13화 static 멤버변수와 싱글톤, 싱글톤 매크로를 이용한 싱글톤 생성
14화 장면관리자와 장면, 타이머

(2020 - 11 - 12)
15화 Layer 정렬 및 위치 구조체

(2020 - 11 - 13)
16화 객체 함수 설계 및 레퍼런스 카운트

### 특이 사항
##### 씬 구성 방식
(13화)
장면(Scene)과 그 장면들을 관리하는 관리자(SceneManager)가 필요하다.
간단하게 생각하면 포토샵의 무수한 레이어들을 배치하고 그걸 합치면 사진이 되듯이

씬
레이어
오브젝트!

레이어 날아가면 그 안에 있는 오브젝트가 다 날아가는 식!

서브 시스템 디자인 : 하나의 메인 시스템이 여러개의 서브 관리자를 관리
그 서브 관리자들은 자기 하위에 있는 객체들을 관리

(15화)
레이어는 장면에서 리스트로 관리된다.
이를 통해서 장면이 날아가면 그 아래에 있는 모든 레이어 및 오브젝트가 삭제된다.
이를 위해서 만든 헬퍼 함수가 Safe_Delete_VecList함수이다.

STL의 sort는 caller가 callee에게 전역함수일 것을 요구한다.
그리고 callee의 조건은 bool 리턴 타입이다.

오브젝트에는 움직임의 유무 정보, 위치, 사이즈 정보가 필요하다.
(이 부분은 물리 책에 좀 더 자세히 나와있다.)

(16화)
플레이어의 정보를 UI에서 표시하고, 몬스터가 때리면 플레이어가 영향을 받는다.
즉 게임을 만들다 보면, 어떤 객체가 다른 객체를 알아야되는 경우가 굉장히 흔하다.
하지만 자료구조를 통해서 실시간을 통해서 찾는다? 굉장히 느리다.

즉 꽤 빠른 시점에 이미 참조를 하게 만드는 것이 좋아보인다.
하지만 플레이어가 죽었는데 UI가 참조하고 있다면, 이는 빈 공간을 참조하는 것이다.
즉 객체를 공유할 땐 이런 문제가 발생한다.

그럼 누군가 무엇을 참조할 때 이 횟수를 알게 하고, 이 수치가 0이 된다면
이를 지우도록 하는 것이다.
	
//////

게임은 4가지의 요소가 있다.
1. 입력
2. 입력받은 토대로 내용 업데이트
3. 그 내용을 토대로 충돌처리
4. 충돌 처리를 기반으로 Render

코어부터 게임오브젝트까지 내려오는 4가지의 요소를 선언함.

(17화)
오브젝트 클래스의 실사용 시작

다양한 물체가 다양한 방식의 레퍼런스 카운터를 가지도록 만든다.

(18화)
생성되면 자동으로 풀에 들어가는 시스템을 만든다.

(19화)
전체 오브젝트들을 통합해서 관리하는 시스템 필요성

/////
VS2015 때와 다르게 PreCompile 부분이 약간 바뀐 점이 있음.
그 부분 때문에 만든 파일이 바로 inl 파일인 점을 참고할 것.

C++은 cpp파일, h파일 외적으로도 inl 파일을 만들고,
거기서 h파일에서 전방선언했던 클래스 중 필요한 클래스의 헤더를
포함하는 방식으로 한다.
/////

과제 : 이거 이용해서 슈팅게임 만들기

## 카피할만한 게임
슈퍼마리오3
메이플스토리
세나